#include <iostream>
#include <string>
using namespace std;
void BaseLevel()
{
    // В заданой строке посчитать количество  букв "А".
    cout << "BaseLevel\n" << endl;
    string arr = "And how аre you?";
    int number = 0;
    for (int i = 0; i < arr.length(); i++)
    {
        if (arr[i] == 'А' || arr[i] == 'а')
        {
            number++;
        }
    }
    cout << number;
    cout << "\n\n";
}

void MiddleLevel()
{
    // В заданой строке заменить подряд идущие пробелы на один пробел.
    cout << "MiddleLevel\n" << endl;
    string arr = "Ds as  asdk       lwad   w      wwae?\n";
    string s;
    int k = 0;
    for (int i = 0; i < arr.length(); i++) {
        if (arr[i] == ' ' && arr[i + 1] == ' ') {
            i++;
        }
        else {
            s += arr[i];
            k++;
        }
    }
    cout << s;
    cout << "\n\n";
}

void HighLevel()
{
    // В заданой строке поменять местами первое и последнее слово строки. Разделителями слов считаются пробелы.
    cout << "HighLevel\n" << endl;
    string arr = "Dsas  asdk lwad  w  wwae";
    string p1;
    string p2;
    for (int i = arr.length() - 1; i >= 0; i--) {
        if (arr[i - 1] == ' ' && arr[i] != ' ' && i != 0) {
            for (int j = i; j < arr.length(); j++) {
                p1 += arr[j];
            }
            i = -1;
        }
    }

    for (int i = 0; i < arr.length(); i++) {
        if (arr[i] != ' ') {
            p2 += arr[i];
            if (arr[i + 1] == ' ') {
                i = arr.length();
            }
        }
    }

    arr.replace(arr.find(p1), p1.length(), p2);
    arr.replace(arr.find(p2), p2.length(), p1);

    cout << arr;
}
int main() {
    BaseLevel();
    MiddleLevel();
    HighLevel();
    return 0;
}

